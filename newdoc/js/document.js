import { db, signedFile } from './supabase.js';
import { shell, escapeHtml, toast, busy } from './ui.js';
import { generatePdf } from './pdf.js';
import { downloadWord } from './word.js';
const user=await shell('สร้างบันทึกข้อความ','document');
if(user){
  const params=new URLSearchParams(location.search);let id=params.get('id'), dirty=false, saving=false, signatureUrl=null, profile={};
  const [{data:profileData},{data:signatures}]=await Promise.all([db.from('profiles').select('*').eq('user_id',user.id).maybeSingle(),db.from('signatures').select('*').order('created_at',{ascending:false})]);profile=profileData||{};
  let doc=null;
  if(id){const {data,error}=await db.from('documents').select('*').eq('id',id).maybeSingle();if(error||!data){toast(error?.message||'ไม่พบเอกสาร',true);id=null}else doc=data}
  document.querySelector('#page-content').innerHTML=`<div class="editor-layout"><div class="editor-panel panel"><div class="form-grid"><label class="span-2">ส่วนราชการ<input id="department" value="${escapeHtml(doc?.department||profile.department||'')}" required></label><label>เลขที่หนังสือ<input id="number" value="${escapeHtml(doc?.document_number)}" placeholder="เช่น กค ๐๐๐๑/๑๒๓"></label><label>วันที่<input id="date" type="date" value="${escapeHtml(doc?.document_date||new Date().toISOString().slice(0,10))}" required></label><label class="span-2">เรื่อง<input id="subject" value="${escapeHtml(doc?.subject)}" required></label><label class="span-2">เรียน<input id="recipient" value="${escapeHtml(doc?.recipient)}" required></label></div><div><label for="editor">เนื้อหาเอกสาร</label><div class="rich-toolbar" id="rich-toolbar" aria-label="เครื่องมือจัดรูปแบบ"><button type="button" data-cmd="bold" title="ตัวหนา"><b>B</b></button><button type="button" data-cmd="italic" title="ตัวเอียง"><i>I</i></button><button type="button" data-cmd="underline" title="ขีดเส้นใต้"><u>U</u></button><button type="button" data-cmd="justifyLeft" title="ชิดซ้าย">≡</button><button type="button" data-cmd="justifyCenter" title="กึ่งกลาง">☷</button><button type="button" data-cmd="justifyRight" title="ชิดขวา">≡</button><button type="button" data-cmd="justifyFull" title="กระจายบรรทัด">☰</button><button type="button" data-cmd="insertUnorderedList" title="รายการหัวข้อ">•</button><button type="button" data-cmd="insertOrderedList" title="รายการลำดับ">1.</button><button type="button" data-cmd="indent" title="เพิ่มระยะย่อหน้า">⇥</button><button type="button" data-cmd="outdent" title="ลดระยะย่อหน้า">⇤</button></div><div id="editor" class="rich-editor" contenteditable="true" role="textbox" aria-multiline="true" aria-label="เนื้อหาเอกสาร"></div></div><div class="form-grid"><label>ชื่อผู้ลงนาม<input id="signer" value="${escapeHtml(doc?.signer_name||[profile.first_name,profile.last_name].filter(Boolean).join(' '))}"></label><label>ตำแหน่ง<input id="position" value="${escapeHtml(doc?.signer_position||profile.position)}"></label><label class="span-2">ลายเซ็น<select id="signature"><option value="">ไม่ใช้ลายเซ็น</option>${(signatures||[]).map(s=>`<option value="${s.id}" ${doc?.signature_id===s.id?'selected':''}>${escapeHtml(s.signature_name)}</option>`).join('')}</select></label></div><div class="editor-actions"><button id="preview-button" type="button">พรีวิว</button><button id="save-button" class="primary" type="button">บันทึกฉบับร่าง</button><button id="pdf-button" type="button">สร้าง PDF</button></div><p id="save-state" class="doc-hint" role="status"></p></div><div class="preview-column"><div class="preview-head"><h2>ตัวอย่างเอกสาร A4</h2><span class="muted">ขนาดจริง 210 × 297 มม.</span></div><div class="paper-scroll"><article id="memo" class="memo-page"><h2 class="memo-title">บันทึกข้อความ</h2><div class="memo-meta"><div class="memo-line"><strong>ส่วนราชการ</strong><span id="p-department"></span></div><div class="memo-line"><strong>ที่</strong><span id="p-number"></span><strong>วันที่</strong><span id="p-date"></span></div><div class="memo-line"><strong>เรื่อง</strong><span id="p-subject"></span></div><div class="memo-line"><strong>เรียน</strong><span id="p-recipient"></span></div></div><div id="p-content" class="memo-content"></div><div class="memo-sign"><img id="p-signature" alt="ลายเซ็น" hidden><p>ลงชื่อ ..............................................</p><p id="p-signer"></p><p id="p-position"></p></div></article></div></div></div><div id="preview-backdrop" class="preview-backdrop"></div>`;
  const memo=document.querySelector('#memo');
  const title=memo.querySelector('.memo-title');
  const heading=document.createElement('div');
  heading.className='memo-heading';
  const garuda=document.createElement('img');
  garuda.className='memo-garuda';
  garuda.src='assets/images/garuda.png';
  garuda.alt='ตราครุฑ';
  heading.append(garuda,title);
  memo.prepend(heading);
  const recipientLine=document.querySelector('#p-recipient').parentElement;
  recipientLine.className='memo-recipient';
  memo.insertBefore(recipientLine,document.querySelector('#p-content'));
  const $=id=>document.getElementById(id),editor=$('editor');
  const wordButton=document.createElement('button');
  wordButton.id='word-button';
  wordButton.type='button';
  wordButton.textContent='ดาวน์โหลด Word';
  $('pdf-button').after(wordButton);
  if(!window.DOMPurify){toast('โหลดระบบตรวจเนื้อหาไม่สำเร็จ กรุณาตรวจสอบอินเทอร์เน็ต',true);editor.contentEditable='false'}
  const clean=html=>window.DOMPurify?DOMPurify.sanitize(html,{ALLOWED_TAGS:['p','div','br','b','strong','i','em','u','ul','ol','li','span'],ALLOWED_ATTR:['style'],FORBID_ATTR:['onerror','onclick']}):'';
  editor.innerHTML=clean(doc?.content||'');
  function render(){const map={department:'p-department',number:'p-number',subject:'p-subject',recipient:'p-recipient',signer:'p-signer',position:'p-position'};for(const [field,target] of Object.entries(map))$(target).textContent=$(field).value||' ';const date=$('date').value;$('p-date').textContent=date?new Intl.DateTimeFormat('th-TH',{day:'numeric',month:'long',year:'numeric'}).format(new Date(`${date}T12:00:00`)):' ';$('p-content').innerHTML=clean(editor.innerHTML);const img=$('p-signature');img.hidden=!signatureUrl;if(signatureUrl)img.src=signatureUrl}
  async function updateSignature(){signatureUrl=null;const selected=(signatures||[]).find(s=>s.id===$('signature').value);if(selected)try{signatureUrl=await signedFile('signatures',selected.signature_url)}catch(error){toast(error.message,true)}render()}
  document.querySelectorAll('.editor-panel input,.editor-panel select').forEach(el=>el.addEventListener('input',()=>{dirty=true;render()}));editor.addEventListener('input',()=>{dirty=true;render()});$('signature').addEventListener('change',updateSignature);
  $('rich-toolbar').addEventListener('mousedown',event=>event.preventDefault());$('rich-toolbar').addEventListener('click',event=>{const btn=event.target.closest('button[data-cmd]');if(!btn)return;editor.focus();document.execCommand(btn.dataset.cmd,false,null);dirty=true;render()});
  async function save(status='draft',silent=false){if(saving)return id;if(!window.DOMPurify)throw new Error('ระบบตรวจเนื้อหายังไม่พร้อม');const required=[['department','ส่วนราชการ'],['date','วันที่'],['subject','ชื่อเรื่อง'],['recipient','ผู้รับ']];for(const [field,name] of required)if(!$(field).value.trim())throw new Error(`กรุณาระบุ${name}`);if(!editor.textContent.trim())throw new Error('กรุณาระบุเนื้อหา');saving=true;busy($('save-button'),true);$('save-state').textContent='กำลังบันทึก...';
    try{const values={user_id:user.id,department:$('department').value.trim(),document_number:$('number').value.trim(),document_date:$('date').value,subject:$('subject').value.trim(),recipient:$('recipient').value.trim(),content:clean(editor.innerHTML),signer_name:$('signer').value.trim(),signer_position:$('position').value.trim(),signature_id:$('signature').value||null,status};const query=id?db.from('documents').update(values).eq('id',id):db.from('documents').insert(values);const {data,error}=await query.select('id').single();if(error)throw error;id=data.id;history.replaceState({},'',`create-document.html?id=${encodeURIComponent(id)}`);dirty=false;$('save-state').textContent=silent?'บันทึกอัตโนมัติแล้ว':'บันทึกเอกสารแล้ว';if(!silent)toast('บันทึกเอกสารเรียบร้อยแล้ว');return id}
    finally{saving=false;busy($('save-button'),false)}
  }
  $('save-button').onclick=async()=>{try{await save()}catch(error){toast(error.message,true);$('save-state').textContent='บันทึกไม่สำเร็จ'}};
  $('preview-button').onclick=()=>{render();document.querySelector('.preview-column').scrollIntoView({behavior:'smooth',block:'start'})};
  $('pdf-button').onclick=async()=>{const button=$('pdf-button');busy(button,true);try{const docId=await save('draft',true);render();const result=await generatePdf($('memo'),docId,user.id);const link=document.createElement('a');link.href=URL.createObjectURL(result.blob);link.download=result.fileName;link.click();setTimeout(()=>URL.revokeObjectURL(link.href),60000);$('save-state').innerHTML=`PDF พร้อมแล้ว <a href="${escapeHtml(result.url)}" target="_blank" rel="noopener">เปิดดู / พิมพ์</a>`}catch(error){toast(error.message,true)}finally{busy(button,false)}};
  wordButton.onclick=async()=>{busy(wordButton,true);try{await downloadWord({department:$('department').value,number:$('number').value,date:$('date').value,subject:$('subject').value,recipient:$('recipient').value,content:clean(editor.innerHTML),signer:$('signer').value,position:$('position').value,signatureUrl});toast('ดาวน์โหลดไฟล์ Word แล้ว')}catch(error){toast(error.message,true)}finally{busy(wordButton,false)}};
  setInterval(async()=>{if(dirty&&!saving){try{await save('draft',true)}catch(error){$('save-state').textContent=`บันทึกอัตโนมัติไม่สำเร็จ: ${error.message}`}}},30000);
  addEventListener('beforeunload',event=>{if(dirty){event.preventDefault();event.returnValue=''}});
  await updateSignature();render();if(params.has('pdf'))toast('ตรวจสอบเอกสารแล้วกดสร้าง PDF');
}
